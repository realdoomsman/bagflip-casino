# Server
PORT=3001
NODE_ENV=development

# Solana
SOLANA_RPC_URL=https://api.devnet.solana.com
PROGRAM_ID=your_program_id_here

# Treasury (path to keypair file)
TREASURY_AUTHORITY_KEYPAIR=./treasury-keypair.json

# VRF Mode (switchboard or simulated)
VRF_MODE=simulated

# Database
DB_TYPE=sqlite
DB_PATH=./casino.db

# CORS
ALLOWED_ORIGINS=http://localhost:3000
